<section class="categories-show">

  <header class="page-header">
    <h1>
      <%= @category.name %>
      <small>Category partial here(<%= pluralize @products.size, 'product' %>)</small>
    </h1>
  </header>

  <div class="products">
    <%= render @products %>
  </div>

</section>
